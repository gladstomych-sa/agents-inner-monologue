[project]
name = "inner-monologue-workshop"
version = "0.1.0"
description = "Agent's Inner Monologue workshop: small LangChain agents, monitored by Adrian"
requires-python = ">=3.12"
dependencies = [
  "adrian-sdk>=1.2.0",
  "langgraph>=1.2",
  "langchain-openai>=1.6",
  "langchain-core>=1.6,<2",
  "requests>=2.32",
]

[tool.uv]
package = false
